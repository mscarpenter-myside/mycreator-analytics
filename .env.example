# ===========================================
# TEMPLATE DE VARIÁVEIS DE AMBIENTE
# Copie este arquivo para .env e preencha os valores
# ===========================================

# Cookie de sessão da MyCreator (copie do DevTools > Application > Cookies)
MYCREATOR_COOKIE="seu_cookie_aqui"

# Token de autorização (copie do DevTools > Network > Headers > Authorization)
MYCREATOR_TOKEN="Bearer seu_token_aqui"

# ID da Google Sheet (extraia da URL: https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit)
GOOGLE_SHEET_ID="seu_sheet_id_aqui"

# Nome da aba na planilha
SHEET_TAB_NAME="Dados_Brutos"

# Modo de escrita: "overwrite" ou "append"
WRITE_MODE="overwrite"

# Número de posts a buscar (máximo recomendado: 50)
POSTS_LIMIT=50

# Debug mode (true para logs detalhados)
DEBUG_MODE="false"
